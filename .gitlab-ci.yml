build:
  services:
  - docker:1.11-dind
  image: docker:1.11
  script:
  - apk add -U make git
  - make build
  - docker login --username gitlab-ci-token --password $CI_BUILD_TOKEN registry.gitlab.com
  - make publish
  - docker logout
